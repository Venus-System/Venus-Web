# Enquanto a API não existe, os serviços devolvem dados falsos.
# true = dados falsos | false = API real
VITE_USAR_MOCK=true

# Endereço da API. Só é lido quando VITE_USAR_MOCK=false.
VITE_API_URL=http://localhost:8080